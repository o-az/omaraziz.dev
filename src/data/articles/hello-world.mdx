---
title: Hello, World!
---

import { testVariable } from '../../data/articles/test1.mdx'

# {title}

---

{testVariable}

#

## Bower

    ```sh
    bower install --save-dev jquery
    ```

## Table

| a   | b   |   c |  d  |
| --- | :-- | --: | :-: |

## Tasklist

- [ ] to do
- [x] done

---

test `hello` world
___
```ts showLineNumbers
const loremFetcher = async (id: string) => {
  const ipsum = await fetch(`.../${id}`)
  const text = await ipsum.text()
  return text
}
```

---

```lua
local function loremFetcher(id: string)
  local ipsum = http.request(`.../${id}`)
  local text = ipsum.text()
  return text
end
```
___
```java
public class LoremFetcher {
  public static String loremFetcher(String id) {
    String ipsum = http.request(`.../${id}`)
    String text = ipsum.text()
    return text
  }
}
```
