```html
<p>Hello, world!</p>
```

```ts

const loremFetcher = async (id: string) => {
  const ipsum = await fetch(`.../${id}`)
  const text = await ipsum.text()
  return text
}


```

```py
def loremFetcher(id):
  ipsum = requests.get(f'.../{id}')
  text = ipsum.text
  return text


```

```c
#include <stdio.h>
// write hello world
void helloWorld() {
  printf("Hello, world!\n");
}
```