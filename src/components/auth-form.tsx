import * as Solid from 'solid-js';

export function AuthForm(props: object) {
  return (
    <form name="auth-form">
      <label>
        <input type="text" id="xyz" />
      </label>
    </form>
  );
}
